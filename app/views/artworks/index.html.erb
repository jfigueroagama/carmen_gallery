<h1>Listing Artworks</h1>

<table>
  <tr>
    <th>Artwork</th>
	<th>Name</th>
	<th>Email</th>
    <th>Art</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @artworks.each do |artwork| %>
  <tr class="artworks">
    <td><%= image_tag artwork.avatar.url(:thumb) %></td>
	<td><%= artwork.name %></td>
	<td><%= artwork.email %></td>
    <td><%= artwork.art %></td>
    <td><%= link_to 'Show', artwork %></td>
    <td><%= link_to 'Edit', edit_artwork_path(artwork) %></td>
    <td><%= button_to 'Delete', artwork, :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Artwork', new_artwork_path %>
